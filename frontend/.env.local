# Route API calls through the CRA dev-server proxy (setupProxy.js) so they are
# same-origin — the Java backend sends no CORS headers, so direct :8080 calls
# are blocked by the browser.
REACT_APP_API_BASE_URL=/api
